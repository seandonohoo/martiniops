---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blackbox-exporter-isp2
spec:
  chartRef:
    kind: OCIRepository
    name: blackbox-exporter-isp2
  interval: 1h
  values:
    fullnameOverride: blackbox-exporter-isp2
    podAnnotations:
      k8s.v1.cni.cncf.io/networks: |-
        [{
          "name": "trusted",
          "namespace": "kube-system",
          "ips": ["10.26.50.250/24"],
          "mac": "3e:d0:83:c0:8e:25"
        }]
    config:
      modules:
        http_2xx:
          prober: http
          timeout: 5s
          http:
            valid_http_versions:
              - HTTP/1.1
              - HTTP/2.0
            follow_redirects: true
            preferred_ip_protocol: ip4
        icmp:
          prober: icmp
          timeout: 5s
          icmp:
            preferred_ip_protocol: ip4
        tcp_connect:
          prober: tcp
          timeout: 5s
          tcp:
            preferred_ip_protocol: ip4
    serviceMonitor:
      enabled: true
      selfMonitor:
        enabled: true
    prometheusRule:
      enabled: true
      rules:
        - alert: Isp2ProbeFailed
          expr: |-
            probe_success{service="blackbox-exporter-isp2"} == 0
          for: 15m
          labels:
            severity: critical
          annotations:
            summary: |-
              The probe targeting {{ $labels.instance }} is currently having connectivity issues
